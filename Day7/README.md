# Day 7: Security, Testing, and Documentation

Adds session-backed CSRF token generation and validation for authentication forms, plus a security regression test. Run it with `C:\xampp\php\php.exe Day7\tests\security_test.php`.

Before production, use HTTPS, set a non-root MySQL account, configure backups, and replace the demo payment selector with a real provider verification callback.
